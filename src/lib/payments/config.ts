import "server-only";
import { tokenPacks,type TokenPack } from "@/config/paymentCatalog";
const environment=process.env.PADDLE_ENV==="live"?"live":"sandbox";
const packIds:Record<number,string|undefined>={5:process.env.PADDLE_TOKEN_PRICE_5_ID,20:process.env.PADDLE_TOKEN_PRICE_20_ID,50:process.env.PADDLE_TOKEN_PRICE_50_ID,100:process.env.PADDLE_TOKEN_PRICE_100_ID,250:process.env.PADDLE_TOKEN_PRICE_250_ID,500:process.env.PADDLE_TOKEN_PRICE_500_ID};
export const paymentConfig={enabled:process.env.MERIDIAN_PAYMENTS_ENABLED==="true",environment,apiKey:process.env.PADDLE_API_KEY||"",clientToken:process.env.NEXT_PUBLIC_PADDLE_CLIENT_TOKEN||"",webhookSecret:process.env.PADDLE_WEBHOOK_SECRET||"",activationPriceId:process.env.PADDLE_PAYG_PRICE_ID||"",packIds,apiBase:environment==="live"?"https://api.paddle.com":"https://sandbox-api.paddle.com"};
export function paymentConfiguration(){const missing:string[]=[];if(!paymentConfig.apiKey)missing.push("PADDLE_API_KEY");if(!paymentConfig.clientToken)missing.push("NEXT_PUBLIC_PADDLE_CLIENT_TOKEN");if(!paymentConfig.webhookSecret)missing.push("PADDLE_WEBHOOK_SECRET");if(!paymentConfig.activationPriceId)missing.push("PADDLE_PAYG_PRICE_ID");for(const key of Object.keys(tokenPacks) as unknown as TokenPack[])if(!packIds[key])missing.push(`PADDLE_TOKEN_PRICE_${key}_ID`);const tokenPrefix=environment==="live"?"live_":"test_";if(paymentConfig.clientToken&&!paymentConfig.clientToken.startsWith(tokenPrefix))missing.push("NEXT_PUBLIC_PADDLE_CLIENT_TOKEN_ENVIRONMENT_MISMATCH");return{...paymentConfig,configured:paymentConfig.enabled&&missing.length===0,missing};}
export function packForPrice(priceId:string){return (Object.entries(packIds).find(([,id])=>id===priceId)?.[0]||null) as `${TokenPack}`|null;}
