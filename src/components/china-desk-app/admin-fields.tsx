import type { InputHTMLAttributes, TextareaHTMLAttributes } from "react";

export function Field({ label, ...props }: InputHTMLAttributes<HTMLInputElement> & { label: string }) { return <label className="block"><span className="eyebrow text-stone">{label}</span><input {...props} className={`mt-3 w-full border-b border-ink/20 bg-transparent pb-3 text-sm outline-none ${props.className||""}`} /></label>; }
export function Textarea({ label, ...props }: TextareaHTMLAttributes<HTMLTextAreaElement> & { label: string }) { return <label className="block"><span className="eyebrow text-stone">{label}</span><textarea {...props} className={`mt-3 w-full resize-y border-b border-ink/20 bg-transparent pb-3 text-sm leading-6 outline-none ${props.className||""}`} /></label>; }
export function SelectField({ label, name, value, options }: { label: string; name: string; value?: string; options: readonly string[] }) { return <label className="block"><span className="eyebrow text-stone">{label}</span><select name={name} defaultValue={value} className="mt-3 w-full border-b border-ink/20 bg-transparent pb-3 text-sm outline-none">{options.map(option=><option key={option}>{option}</option>)}</select></label>; }
export function Submit({ children="Save" }: { children?: string }) { return <button className="border-b border-ink/25 pb-1.5 text-sm font-medium">{children} →</button>; }
export function OrganizationField({ organizations, value }: { organizations: Array<{id:string;name:string}>; value?: string }) { return <label className="block"><span className="eyebrow text-stone">Client</span><select required name="organization_id" defaultValue={value} className="mt-3 w-full border-b border-ink/20 bg-transparent pb-3 text-sm outline-none"><option value="">Select client</option>{organizations.map(org=><option key={org.id} value={org.id}>{org.name}</option>)}</select></label>; }
